<%- include("./partials/header.ejs") %> <!-- Adds partial called header -->

    <!-- Line 4 to 8 is conditionally rendering the data passed from the backend -->
    <% if (locals.randomAdj && locals.randomNoun) {  %>
      <h1><%= randomAdj %> <%= randomNoun %></h1>
    <% } else { %>
      <h1>Welcome to Band Name Generator</h1>
    <% } %>

<form action="/submit" method="POST">
  <input type="submit" value="Generate Name">
</form>

<%- include("./partials/footer.ejs") %> <!-- Adds partial called footer -->